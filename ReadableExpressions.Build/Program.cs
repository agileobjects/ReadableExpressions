namespace AgileObjects.ReadableExpressions.Build
{
    using SourceCode;

    /// <summary>
    /// Provides an entry point for source-code generation.
    /// </summary>
    public class Program
    {
        /// <summary>
        /// Compiles a <see cref="SourceCodeExpression"/> from a configured method into a source
        /// code file at a configured destination.
        /// </summary>
        /// <param name="args">Arguments passed to the method.</param>
        public static void Main(string[] args)
        {
        }
    }
}
